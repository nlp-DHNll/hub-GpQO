###    作业2: 阅读意图识别 01-intent-classify 代码，梳理源文件的作用，绘制从fastapi 接受请求到到返回结果的流程，绘制流程图（手绘、自然语言表达）。

> 1 收到请求，拿到要分类的文本
> 
> 2 用 TF-IDF 从训练集里找出最相似的 10 条带标签的例子
> 
> 3 把「候选类别 + 例子 + 待分类文本」拼成提示词，发给大模型
> 
> 4 大模型返回类别，装进响应里（带上耗时）返回给客户端